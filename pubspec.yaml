name: babylonjs_viewer
description: 3D Model Rendering with BabylonJS Viewer
version: 1.3.0
repository: https://github.com/MarlonJD/babylonjs_viewer
homepage: https://github.com/MarlonJD/babylonjs_viewer

environment:
  sdk: '>=2.12.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^4.10.0

dev_dependencies:
  flutter_test:
    sdk: flutter

# The following section is specific to Flutter.
flutter:
  assets:
    - assets/viewer/